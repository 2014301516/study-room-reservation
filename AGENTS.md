## Agent skills

### Issue tracker

GitHub Issues，通过 `gh` CLI 操作。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五标签词汇表（needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix）。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文（single-context）布局：`CONTEXT.md` + `docs/adr/` 放在项目根目录。详见 `docs/agents/domain.md`。
